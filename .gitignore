# Compiled database — regenerate locally with scripts/migrate_v3_to_v3.1.py
# (114 MB, over GitHub's 100 MB push limit without Git LFS — see README)
*.db
*.sqlite
*.sqlite3

output/

# Data-steward curation notes -- working file with informal in-progress
# review notes, stays local-only, never pushed
DS_scripts/logs/

# Local geospatial reference data for release_finalization scripts (e.g. the
# WOKAM shapefile, Copernicus CGLS-LC100 raster tiles) -- too large / not
# ours to redistribute. Each steward downloads their own copy; see the
# README.md in each of these folders (kept -- everything else ignored).
DS_scripts/release_finalization/data/*
!DS_scripts/release_finalization/data/README.md
!DS_scripts/release_finalization/data/copernicus_lcc/
DS_scripts/release_finalization/data/copernicus_lcc/*
!DS_scripts/release_finalization/data/copernicus_lcc/README.md

.DS_Store

__pycache__/
*.pyc

# Local Claude Code working conventions -- stays local-only for now
CLAUDE.md
